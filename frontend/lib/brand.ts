/** App display name — override with NEXT_PUBLIC_APP_NAME on deploy (e.g. Hjerterum). */
export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME?.trim() || 'Hjerterum'

export const APP_TAGLINE_NO =
  'Trygt bindeledd mellom kommune og private utleiere i Nord-Norge.'
